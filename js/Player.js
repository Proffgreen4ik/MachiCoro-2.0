class Player {
    constructor (money = 3,cards = [],queue = 0) {
        this.money = money
        this.cards = cards
        this.queue = queue
    }
    throw (obj) {
        for (let i = 0;i<attempts+1;i++) {
            obj.beThrown()
        }
        return
    }
}